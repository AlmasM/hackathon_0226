# Get a key from Google Cloud Console → APIs & Services → Credentials.
# Enable "Maps JavaScript API" for the project that owns this key.
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-key-here

# Backend API base URL (optional; defaults to http://localhost:8000 when unset)
# VITE_API_BASE_URL=http://localhost:8000
